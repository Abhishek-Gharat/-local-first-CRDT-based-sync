services:
  - type: web
    name: docsync-sync-server
    runtime: docker
    dockerfilePath: ./apps/sync-server/Dockerfile
    plan: free
    envVars:
      - key: SYNC_TOKEN_SECRET
        generateValue: true
