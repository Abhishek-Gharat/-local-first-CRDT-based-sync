# docsync

Local-first collaborative document editor. Work in progress — see commit history for progress.

## Getting started

```bash
pnpm install
pnpm db:up          # starts local postgres via docker compose
pnpm dev            # starts the web app
```

Requires Node 20+, pnpm 10+, and Docker.
